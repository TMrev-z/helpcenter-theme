{{> header}}
<main>
  <div class="category-page">
    <h1>{{category.name}}</h1>
    <div class="articles">
      {{#each articles}}
        <div class="article-item">
          <h3><a href="{{url}}">{{title}}</a></h3>
        </div>
      {{/each}}
    </div>
  </div>
</main>
{{> footer}}