<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{help_center.name}}{{#if help_center.name}} - {{/if}}{{#if section}}{{section.name}} - {{/if}}{{#if article}}{{article.title}}{{else}}ヘルプセンター{{/if}}</title>
  
  {{!-- CSS変数設定 --}}
  <style>
    :root {
      --bg-dark: #1a1a2e;
      --bg-body: #121218;
      --text-primary: #FFFFFF;
      --text-secondary: #B8BCC8;
      --accent: {{settings.accent_color}};
      --category-1: {{settings.category_color_1}};
      --category-2: {{settings.category_color_2}};
      --category-3: {{settings.category_color_3}};
      --category-4: {{settings.category_color_4}};
      --category-5: {{settings.category_color_5}};
      --category-6: {{settings.category_color_6}};
    }
  </style>
  
  {{!-- メインスタイルシート --}}
  <link rel="stylesheet" href="{{asset 'style.css'}}?v={{settings.theme_version}}">
  
  {{!-- JavaScript --}}
  <script src="{{asset 'script.js'}}?v={{settings.theme_version}}"></script>
</head>